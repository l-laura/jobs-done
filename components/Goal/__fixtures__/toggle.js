import { StatefulGoal } from '../StatefulGoal';
import { Goal } from '..';

export default {
  component: Goal,
  decorator: StatefulGoal,
  bg: true,
  viewport: {
    width: 411,
    height: 731
  }
};
